from typing import List




class KthLargest:

    root = None

    def __int__(self, a = 3, b =4, *args,**kwargs):
        pass

    def add_element(self, node: TreeNode, value: int):
        if value < node.value:
            if node.left is None:
                node.left = TreeNode()


    def __init__(self, k: int, nums: List[int]):
        self.root = TreeNode(nums[0], None, None)
        for i in range(1, len(nums)):





    def add(self, val: int) -> int:
        pass